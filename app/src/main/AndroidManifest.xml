<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bootcamp.inviobootcampbp_sepette">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".di.HiltApplication"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/sepette"
        android:label="Sepette"
        android:roundIcon="@mipmap/sepette"
        android:supportsRtl="true"
        android:theme="@style/Theme.InvioBootcampBPSepette"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity.OnBoardingActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activity.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.LoginActivity"
            android:exported="true"></activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>